<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      height: 100vh;
      margin: 0;
      background: linear-gradient(to right, #6a11cb, #2575fc);
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .card {
      width: 100%;
      max-width: 400px;
      border: none;
      border-radius: 15px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
    }
    .form-control:focus {
      box-shadow: none;
      border-color: #6a11cb;
    }
    .btn-primary {
      background-color: #6a11cb;
      border: none;
    }
    .btn-primary:hover {
      background-color: #5a0fb2;
    }
  </style>
</head>
<body>
  <div class="card p-4">
    <h2 class="text-center mb-4">Login</h2>
    <form action="/auth/login" method="POST">
      <div class="mb-3">
        <input type="email" name="email" class="form-control" placeholder="Email" required />
      </div>
      <div class="mb-3">
        <input type="password" name="password" class="form-control" placeholder="Password" required />
      </div>
      <div class="d-grid">
        <button type="submit" class="btn btn-primary">Login</button>
      </div>
    </form>
    <div class="mt-3 text-center">
      <a href="/auth/register" class="text-decoration-none text-primary">Don't have an account? Register</a>
    </div>
  </div>
</body>
</html>

<!-- <!DOCTYPE html>
<html>
<head><title>Login</title></head>
<body>
  <h1>Login</h1>
  <form action="/auth/login" method="POST">
    <input name="email" type="email" placeholder="Email" required />
    <input name="password" type="password" placeholder="Password" required />
    <button type="submit">Login</button>
  </form>
  <a href="/auth/register">Don't have an account? Register</a>
</body>
</html> -->

<!-- <!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>Login - Finance Tracker</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">

  <style>
    :root {
      --bg-light: linear-gradient(145deg, #f5f8ff, #dbeaff);
      --bg-dark: #121212;
      --box-light: #ffffff;
      --box-dark: #1e1e1e;
      --text-light: #1e2a38;
      --text-dark: #f5f5f5;
      --subtext-light: #4b5563;
      --subtext-dark: #cbd5e1;
      --btn-blue: #0070f3;
      --btn-blue-hover: #0058cc;
    }

    body {
      margin: 0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      transition: background 0.3s ease, color 0.3s ease;
    }

    body.light-mode {
      background: var(--bg-light);
      color: var(--text-light);
    }

    body.dark-mode {
      background: var(--bg-dark);
      color: var(--text-dark);
    }

    .login-box {
      background-color: var(--box-light);
      padding: 40px 30px;
      border-radius: 16px;
      max-width: 400px;
      width: 90%;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
      border: 1px solid #e0e7ff;
      transition: background 0.3s ease;
    }

    body.dark-mode .login-box {
      background-color: var(--box-dark);
      border-color: #333;
    }

    h2 {
      margin-bottom: 20px;
      font-weight: 700;
    }

    label {
      font-weight: 500;
      margin-bottom: 6px;
    }

    .form-control {
      border-radius: 12px;
      padding: 10px 14px;
      font-size: 1rem;
      margin-bottom: 16px;
    }

    .btn-login {
      background-color: var(--btn-blue);
      color: #fff;
      font-weight: 600;
      border-radius: 25px;
      padding: 10px 20px;
      width: 100%;
      transition: 0.3s ease;
    }

    .btn-login:hover {
      background-color: var(--btn-blue-hover);
    }

    .register-link {
      display: block;
      margin-top: 18px;
      font-size: 0.95rem;
      text-align: center;
    }

    .mode-toggle {
      position: absolute;
      top: 20px;
      right: 20px;
      cursor: pointer;
      font-size: 1.4rem;
      color: #888;
    }

    body.dark-mode .mode-toggle {
      color: #ccc;
    }
  </style>
</head>

<body class="light-mode">

  Dark Mode Toggle
  <div class="mode-toggle" onclick="toggleMode()" title="Toggle Dark Mode">
    <i id="modeIcon" class="fas fa-moon"></i>
  </div>

  Login Form
  <div class="login-box">
    <h2>Login</h2>
    <form action="/auth/login" method="POST">
      <div class="mb-3">
        <label for="email">Email</label>
        <input name="email" id="email" type="email" class="form-control" required />
      </div>
      <div class="mb-3">
        <label for="password">Password</label>
        <input name="password" id="password" type="password" class="form-control" required />
      </div>
      <button type="submit" class="btn btn-login">Login</button>
    </form>
    <a href="/auth/register" class="register-link">Don't have an account? Register</a>
  </div>

  Dark Mode Script
  <script>
    function toggleMode() {
      const body = document.body;
      const icon = document.getElementById('modeIcon');
      body.classList.toggle('dark-mode');
      body.classList.toggle('light-mode');
      icon.classList.toggle('fa-moon');
      icon.classList.toggle('fa-sun');
    }
  </script>

</body>

</html> -->